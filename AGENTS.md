# 5분 AI 뉴스 — 편집 지침

이 레포의 글은 **한국어 데일리 브리핑**이다. 영어 RSS를 번역하지 말고, 공개된 회사·수치·날짜만 사실로 삼아 우리 문장으로 쓴다. 원문 사이트 문장을 베끼지 않는다.

자세한 말투·예시는 [`content/VOICE.md`](content/VOICE.md). 스키마는 [`content/schema/issue.schema.json`](content/schema/issue.schema.json). 파이프라인은 [`scripts/pipeline/README.md`](scripts/pipeline/README.md).

## 하루 호

- 평일만. `content/issues/YYYY-MM-DD.json` 하나 = 하루 1호.
- 브리핑 8~12, 분석은 브리핑 id와 1:1. extras는 맨 마지막 한 칸.
- 헤드라인은 `누가 무엇을 했다` 과거형. 가능하면 같은 줄에 숫자.
- 시사점은 **두 문장**. 1문장 판단 + 1문장 숫자 있는 다음 행동. `입니다/됩니다`. `하십시오` 금지.
- 본문 2~3단. 회사 발표와 외부 측정을 한 단에 섞지 않는다. 누가 어디서 쟀는지 밝힌다.
- `sources.url`은 특정 문서·게시물. `example.com`·계정 홈만 있는 칸은 미발행.
- 확인 안 된 인수·점수는 `미확정`. 과장 형용사(혁명, 폭발) 대신 숫자.

## 하지 않는 말

- `칸에 적으면`, `칸을 나눠`, `내부 메모에 올리면` — 신문이 아니라 편집 메모다.
- 시사점 다섯 칸이 모두 `하면 됩니다`로 끝나기.
- GLM이 만든 동일 시사점 복붙.

## 파이프라인

평일 05:30 KST GitHub Action이 수집 → GLM 초안 → `validate.py` 통과 시 **main 직푸시**. 손으로 고친 호(`draft` 없음)는 덮지 않는다.

초안이 위 규칙을 깨면 배포하지 않는다. 고쳐서 다시 돌린다.
